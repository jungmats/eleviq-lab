# dependencies
node_modules/

# Cloudflare Wrangler / Pages local state
.wrangler/
.dev.vars

# build output — the browser bundle is regenerated by `npm run build`
# (Cloudflare Pages runs this on deploy)
public/assets/agent-sign.js
public/assets/agent-sign.js.map
dist/

# local preview screenshots (not part of the site)
.preview/

# OS / editor cruft
.DS_Store
*.log
