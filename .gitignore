# dependencies
node_modules/

# Cloudflare Wrangler local state
.wrangler/
.dev.vars

# local preview screenshots (not part of the site)
.preview/

# OS / editor cruft
.DS_Store
*.log

# NOTE: docs/assets/agent-sign.js, docs/assets/charge-sign.js,
# docs/reference/*.jwk.json, and docs/reference/charge-agent.json are build
# outputs but ARE committed — GitHub Pages serves committed files directly.
# Regenerate with `npm run build` and commit the result.
